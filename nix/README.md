# Nix

This directory will contain the macOS configuration managed with nix-darwin,
Home Manager, and nix-homebrew.

The existing GNU Stow and Homebrew setup remains active while its configuration
is migrated here incrementally.
