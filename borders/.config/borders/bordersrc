#!/bin/bash

# Match AlvaroParker/config's Hyprland active-window palette on macOS.
options=(
  style=round
  width=2.0
  hidpi=on
  "active_color=gradient(top_left=0xff89b4fa,bottom_right=0xffcba6f7)"
  inactive_color=0x9945475a
)

borders "${options[@]}"
